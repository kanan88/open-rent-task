/* eslint-disable @next/next/no-img-element */
import styles from './logo.module.css';

const Logo = () => {
  return (
    <h2 className={styles.logo}>
      <span>OpenRent</span>
    </h2>
  );
};

export default Logo;
